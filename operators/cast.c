#include <stdio.h>

int main() {
  int x = 7;
  int y = 5;
  printf("Given x = %d, y = %d\n", x, y);
  printf("x / y produces: %d\n", x/y);
  printf("(float)x / y produces: %f\n", (float)x / y);
  return 0;
}