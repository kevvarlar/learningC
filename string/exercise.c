#include <stdio.h>
#include <string.h>

int main() {

    // Exercise 1

    // char str1[] = "This is exercise 1";
    // char str2[19];
    // strcpy(str2, str1);
    // printf("%s\n", str2);

    // // Exercise 2

    // int i;
    // int count = 0;
    // for (i = 0; str1[i]; i++) count += sizeof(str1[i]);
    // printf("%d bytes, %ld bytes\n", count, strlen(str1));

    // // Exercise 3

    // char str[80];
    // int delt = 'a' - 'A';
    // printf("Enter a string less than 80 characters:\n");
    // gets(str);
    // i = 0;
    // while (str[i]) {
    //     if ((str[i] >= 'A') && (str[i] <= 'Z')) str[i] += delt;
    //     ++i;
    // }
    // printf("The entered string is (in lowercase):\n");
    // puts(str);

    // // Exercise 4

    // printf("Insert two integers separated by a space:\n");
    // int x, y;
    // scanf("%d %d", &x, &y);
    // printf("%d + %d = %d\n", x, y, x + y);
    // return 0;
}